The Heavyar Cloudflare deployment credential has been replaced with a fresh account-owned API token.

The token is scoped ONLY to:

Worker:
heavyar-api

Permission:
Individual Worker → Editor

Please resume exactly from the blocked deployment point.

Do NOT redo completed implementation or Firestore backfills.

First:

1. Verify CLOUDFLARE_API_TOKEN is active.
2. Verify CLOUDFLARE_ACCOUNT_ID resolves to the Heavyar account.
3. Verify the token has Editor access to the EXISTING heavyar-api Worker.
4. Verify it can read the current Worker version/settings.
5. Do not create a new Worker.
6. Do not modify another Cloudflare resource.

Then:

7. Deploy the already-reviewed Worker source to the existing:
   heavyar-api

8. Preserve ALL existing bindings, variables, secrets, routes, cron triggers, and configuration.

9. Confirm a new Worker version ID was created.

10. Run safe production smoke tests for:
   - /health
   - authenticated admin session
   - XLSX exports
   - public request/equipment number responses
   - staff endpoints
   - equipment moderation
   - campaigns
   - payment gateway status
   - Nafath/Rabet readiness status

11. Keep:
   - Tap LIVE disabled
   - Nafath/Rabet disabled
   - Moyasar disabled unless a real adapter exists
   - MyFatoorah disabled unless a real adapter exists

12. Do NOT perform:
   - real payment
   - refund
   - real Nafath verification
   - EAS build
   - store submission

13. Only after the production Worker smoke tests pass, deploy the already-reviewed Admin build to the existing Firebase Hosting site:

   https://heavyar-app.web.app

14. Verify the LIVE Admin against the LIVE Worker, including:
   - Users
   - Staff & Permissions
   - Providers
   - Drivers
   - Equipment
   - Requests
   - Payments
   - Invoices
   - Refunds
   - Complaints
   - Verification
   - Notifications
   - Marketing Campaigns
   - Payment Gateways
   - Identity Integrations / Nafath
   - Security / Ownership
   - XLSX export

15. Preserve the existing responsive sidebar fix.

16. Do not run the public-ID backfill again unless verification requires a read-only confirmation.
   It already completed successfully:
   - 9 request numbers assigned
   - 5 equipment numbers assigned
   - second pass assigned 0

17. Re-run only the relevant post-deployment regression/security checks.

18. If no source change is necessary, do not create an unnecessary commit.
    Current reviewed Git SHA is:

    80418b621d41ea42efd63efaf608d180ef0e4ee7

Return:
- Cloudflare token authorization result
- previous Worker version
- new Worker version
- bindings/secrets preservation result
- production smoke-test results
- XLSX production verification
- Hosting deployment result
- live Admin verification
- final Git SHA
- any remaining blocker

Then STOP.